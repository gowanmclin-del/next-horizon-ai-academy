import { getReadinessAssessments } from "@/lib/data/admin";
export default async function ReadinessAdminPage({ searchParams }: { searchParams: Promise<{ status?: string }> }) {
  const f=await searchParams; const rows=await getReadinessAssessments(f.status);
  return <div><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-horizon-blue">Corporate partnerships</p><h1 className="mt-1 font-heading text-2xl font-extrabold text-horizon-navy sm:text-3xl">AI Workforce Readiness</h1></div><a href="/admin/corporate" className="text-sm font-bold text-horizon-blue">← Partnership Pipeline</a></div>
    <form className="mt-6 flex gap-2"><select name="status" defaultValue={f.status??""} className="rounded-md border border-slate-300 bg-white px-3 py-2.5 text-sm"><option value="">Any follow-up status</option>{["new","reviewed","contacted","converted","closed"].map(s=><option key={s} value={s}>{s}</option>)}</select><button className="rounded-md bg-horizon-blue px-5 py-2.5 text-sm font-bold text-white">Filter</button></form>
    {rows.length===0?<p className="mt-6 text-sm text-slate-500">No readiness assessments found.</p>:<div className="mt-6 overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr className="border-b text-xs font-bold uppercase tracking-wide text-slate-400"><th className="px-4 py-3">Organization</th><th className="px-4 py-3">Contact</th><th className="px-4 py-3">Score</th><th className="px-4 py-3">Level</th><th className="px-4 py-3">Follow-up</th><th className="px-4 py-3">Received</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-b border-slate-100 last:border-0 hover:bg-horizon-cloud"><td className="px-4 py-3"><a href={`/admin/corporate/readiness/${r.id}`} className="font-bold text-horizon-blue hover:underline">{r.organizationName}</a></td><td className="px-4 py-3"><div>{r.contactName}</div><div className="text-xs text-slate-500">{r.email}</div></td><td className="px-4 py-3 text-xl font-extrabold text-horizon-navy">{r.score}</td><td className="px-4 py-3 font-semibold">{r.level}</td><td className="px-4 py-3 capitalize text-slate-600">{r.followUpStatus}</td><td className="px-4 py-3 text-slate-500">{new Date(r.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div>}
  </div>;
}
